# ── Dependencies ──
node_modules/

# ── Build artifacts (generated by esbuild; distributed via GitHub Releases) ──
main.js
main.css
styles.css
lib/

# ── Obsidian plugin data (NEVER commit user data / credentials) ──
data.json
.obsidian/

# ── Environment / secrets ──
.env
.env.*
*.local

# ── Test artifacts ──
node_modules/.vitest/
*.timestamp-*.mjs

# ── OS / editor noise ──
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Backup / temp ──
*.bak
*.log
*.tmp
